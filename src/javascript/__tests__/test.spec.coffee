# Test = require './../src/javascript/test.coffee'
# Test = require '../test.coffee'
# 
# describe "A thing", ->
#   it "is true or false", ->
#     (true).should.equal true
# 
#   it "works with requires", ->
#     (Test.init()).should.equal 'okay'
# 
#   # it "tests something else", ->
#   #   expect(Test.foo()).toEqual true
